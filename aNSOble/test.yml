---
 - hosts: all
   tasks:
     - block:         
        - shell: whoami
          register: user
        - debug:
             msg: "Logged in user is {{user.stdout}}"   
       tags: mytag  
     - include_role:
            name: data_collector
       tags: data_collector     
