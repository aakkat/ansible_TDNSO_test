---
 - hosts: all
   tasks:
     - shell: whoami
       register: user
     - debug:
           msg: "Logged in user is {{user.stdout}}"   
     - { role: data_collector, tags: [ 'data_collector' ] }
